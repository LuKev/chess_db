{
  "name": "chess_db",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @chessdb/web run dev",
    "dev:api": "npm --workspace @chessdb/api run dev",
    "dev:worker": "npm --workspace @chessdb/worker run dev",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

